el select 3 para las funciones de grupo es importante, tiene funciones como MAX(), MIN()
CONCAT(va1,va2), AVG(), COUNT(), ROUND(AVG())-> PARA QUE NO TENGA DECIMALES
Todas usan se agrupan con GROUP BY pero hay que saber como agruparlas.
